(require 'anything-config)

;;------------------------------------------------
;; アルファベット1文字で選択可能に
;;------------------------------------------------
;;(setq anything-enable-shortcuts 'alphabet)
(setq anything-enable-shortcuts nil)
